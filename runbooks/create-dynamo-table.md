# Runbook: Create DynamoDB Table

This runbook describes how to create the `Inventory` table in DynamoDB.

## Prerequisites
- AWS CLI installed and configured.
- If using Localstack, ensure `awslocal` is installed or use the `--endpoint-url` flag.

## Command

Run the following command to create the table:

